# JeTank ros2_control Controller Configuration
# Updated for 5-DOF arm + gripper

controller_manager:
  ros__parameters:
    update_rate: 50  # Hz

    # Joint State Broadcaster - publishes all joint states to /joint_states
    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    # Arm Controller - 5-DOF Joint Trajectory Controller
    arm_controller:
      type: joint_trajectory_controller/JointTrajectoryController

    # Gripper Controller - Position controller for parallel gripper
    gripper_controller:
      type: position_controllers/JointGroupPositionController


# ============================================
# ARM CONTROLLER (5-DOF)
# ============================================
arm_controller:
  ros__parameters:
    joints:
      - S1_joint
      - S2_joint
      - S3_joint
      - S4_joint
      - S5_joint

    command_interfaces:
      - position

    state_interfaces:
      - position
      - velocity

    # Trajectory execution settings
    state_publish_rate: 50.0
    action_monitor_rate: 20.0
    allow_partial_joints_goal: false
    open_loop_control: false

    constraints:
      stopped_velocity_tolerance: 0.01
      goal_time: 0.0  # 0 = use trajectory timing

      # Per-joint trajectory and goal tolerances
      S1_joint:
        trajectory: 0.1   # rad tolerance during trajectory
        goal: 0.05        # rad tolerance at goal
      S2_joint:
        trajectory: 0.1
        goal: 0.05
      S3_joint:
        trajectory: 0.1
        goal: 0.05
      S4_joint:
        trajectory: 0.1
        goal: 0.05
      S5_joint:
        trajectory: 0.1
        goal: 0.05


# ============================================
# GRIPPER CONTROLLER
# ============================================
gripper_controller:
  ros__parameters:
    joints:
      - gripper_left_joint
      - gripper_right_joint

    command_interfaces:
      - position

    state_interfaces:
      - position
      - velocity

    # Interface names for sending commands
    interface_name: position
